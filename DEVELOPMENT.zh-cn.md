# 开发指南

这是一份关于自己拓展、定制和调试该软件的指南书。

---

## 基本思想

这个软件是人类在向AI代理指示和吐槽的过程中制作的。
代码的每一行都是由AI写的。

也就是说：**你也能做同样的事。**

你不需要是个程序员。也不需要依靠作者。
你只需要具备清晰的思维、准确的表达和重复尝试的意愿。

那种黑底白字的屏幕滚动不是必需的。
首先，要舍弃这种固有观念和偏见。
我们身处一个好时代，所有的事都能以可视化的方式完成。

---

## 开始前

### 需要先读的内容

如果你想添加或修改API，先读一下[API安全指南](docs/ja/help/developer/api-security.md)。

- GET请求也不能过度开放
- read-only API key中应保留哪些内容
- localhost判定、secret、配置API、验证的标准

不读这份指南就增加route，你会重复同样的错误。

### 获取YU AI Manager

只需要运行安装程序。
然后按照屏幕上的说明操作。就这样结束。

只需记住一件事。目前还没有自动更新功能。如果出了新版本，按同样的方式运行安装程序进行替换。

### 连接MCP

打开YU AI Manager，进到**Settings → API Keys**。
有一个叫**MCP Connection Snippet**的部分。把那里的JSON一键复制。

然后打开Claude Desktop，进到**Settings（齿轮图标）→ Developer → Edit Config**。
粘贴复制的JSON，保存并重启Claude Desktop。

就这样。只需这些就能连接。

**关于API密钥：** 如果不用Snippet而是手动设置，同样在**Settings → API Keys**中创建密钥。以`sk_...`开头的密钥只会在创建后显示一次。必须在那时立即复制。

### 确认环境

1. YU AI Manager是否在运行 — 启动并确认
2. MCP服务器是否在运行 — 在Claude Desktop的设置中确认
3. AI代理是否可用 — Claude Desktop或相同的工具

就这样。准备完成。

### 启用Git钩子（仅限用git开发和push的节点、克隆后只需一次）

如果你用git来处理这个仓库（提交或push），那么克隆后只需执行一次这个：

```bash
git config core.hooksPath .githooks
```

这样`.githooks/`中的pre-commit / pre-push检查（lint、类型检查、各种漂移检查）就会启用。
**不设置的话检查会休眠。** 即使逻辑正确，作为关卡也不会运作，漂移混入也会放过（实际上这就是导致文档索引大量偏差的原因）。

确认方式很简单。如果返回`.githooks`就没问题。

```bash
git config --get core.hooksPath
```

每个节点都需要做一次（git的这个配置是针对每个克隆的本地配置，不会共享）。

---

## 使用MCP

如果MCP服务器在运行，就一定要用它。

YU AI Manager为AI代理提供了帮助端点。
通过MCP，你可以直接访问数据库、日志、配置，以及**源代码本身**。
相比通过浏览器UI的说明，用MCP直接展示会更快更准确。

只需这样告诉AI代理：

```
请连接YU AI Manager的MCP服务器。
查看帮助端点，告诉我能做什么。
```

### 通过MCP读源代码

YU AI Manager内置了源代码参考工具。

- **source_tree** — 用树状结构显示文件组织
- **source_read** — 读取指定文件的内容
- **source_search** — 全源代码文本搜索

AI代理可以用这些工具在聊天中直接读源代码。不需要在GitHub Desktop中打开文件夹然后交给Claude Code这样的步骤。

当你想让它参考源代码时，可以这样说：

```
用source_tree确认文件组织，
然后用source_read读取相关的文件。
```

---

## 添加功能

不要让作者为core添加功能。答案是不行。

使用Extension系统。
**所有工作都可以在Claude Desktop的聊天中完成。** 你不需要离开座位。

### 第1步：在聊天中决定要做什么

不要直接说"做吧"。

首先，在Claude Desktop的聊天中，整理一下你想要什么。
"我想要这样的功能""我想自动化这样的操作"——在和AI的对话中表达这些。

当你在脑子里清楚了要做什么，就这样说：

```
请创建一份规格书。
```

AI会创建规格书。

### 第2步：直接让它实现

不需要转到工作场所。在同一个聊天中继续这样说：

```
规格书完成了。请作为Extension来实现。
用create_extension创建模板，用write_extension_file写代码。
用validate_extension确认没有问题。
```

AI通过MCP直接创建和编辑Extension文件。
你的办公桌上，一切都只在聊天中完成。

**但是，是否按照这样做的决定权在你手上。**

把AI的提议当作参考。但你没有义务遵循。
有目标的是你，不是AI。
不要把判断权交出去。

达成共识后就让它实现。如果有问题就告诉它。重复直到能用为止。

Extension完成后重启YU AI Manager。
在Settings → Extensions中会出现新的Extension。确认权限后批准就能用。

### 第3步：分享（可选）

如果你做的东西有用，可以分享。
是否使用由他人决定。We made, you decide.

---

## 报告漏洞

### 第1步：获取日志

打开YU AI Manager，进到**Settings → Logs**。
复制问题发生前后的日志。

如果找不到日志，请准确描述以下内容：
- 你做了什么
- 你认为会发生什么
- 实际发生了什么

"有点不对劲"不是描述。

### 第2步：拍摄截图或视频

如果问题是视觉上的，用语言无法解释：

- **截图**: `Windows + Shift + S`
- **录屏**: `Windows + Shift + R`

Mac的情况：截图是`Cmd + Shift + 4`，录屏是`Cmd + Shift + 5`

你可以直接把图像拖到聊天中。
一张图胜千言，比混乱的说明有价值得多。

**你也可以共享浏览器内部的情况。**

在浏览器中按`F12`。屏幕边缘会打开一个面板。
现在不需要理解是什么意思。只要记住。

当AI代理对你说"打开F12查看错误"时，说的就是这里。
如果有红的和黄的东西，全部选中复制，直接给代理。
就这样。

### 第3步：粘贴到GitHub

把日志和截图粘贴到GitHub的issue中。
作者也许会看到。总有一天。不保证。

如果想立即修复，可以转到下一节。

---

## 自己修复漏洞（推荐）

比等作者更快。真的。

### 工具

**Claude Desktop的聊天 + MCP。** 就这些。

思考、调查、修复——全都在这里完成。
用MCP读写Extension文件，也能运行代码扫描。
不需要其他任何东西。

### 调试流程

在Claude Desktop的聊天中描述问题。
日志、截图、你在做什么、你期望发生什么——全部丢进去。

如果有MCP，AI就能直接读源代码，确认系统的实际状态。告诉它：

```
在YU AI Manager中点击[X]会发生[Y]。本来应该是[Z]。
用MCP确认后端的日志和状态。
用source_tree和source_read读取相关的源代码。
找出原因并提供修复方案。
```

AI找出原因并提供修复方案。
用write_extension_file应用修复，用validate_extension确认。
重启YU AI Manager并确认运行情况。

### 给AI代理的内容

1. **错误日志** — 原文本，不是转述
2. **截图或视频** — 视觉漏洞的情况
3. **你在做什么** — 问题发生时的操作
4. **你期望发生什么** — 本来应该怎样
5. **目的** — 不仅仅是症状，还有为什么需要这个

### 当AI理解不了的时候

AI不是人。它不会补充你遗漏的部分。

- 它可能会反问 — 准确地回答
- 可能不会按你的想法运作 — 准确说出有什么不同
- 如果持续答非所问，就换个说法
- 如果发现信息不足，就补充
- 如果用语言说不清楚，就提交相关文件

这是一个迭代过程。它会起作用。坚持下去。

这和给人类下指令本质上是一样的。只是不涉及面子、心情和感情，所以简单得多。

---

## 先把看得见的地方整理干净

在修复看不见的漏洞之前，先收拾看得见的地方。
雜草遍地的操场上洒灭虫剂也没用。先要整理地面。

你实现了一些东西。看起来能正常工作。但外观是否真的正确运作，自己动鼠标很难分辨。会忽略。习惯了就注意不到。

使用Playwright。AI代理会实际操作浏览器，彻底确认UI。

这样告诉AI代理：

```
用Playwright操作YU AI Manager，进行UI/UX漏洞检查、
UX方面的评价和改进建议。
```

AI会在操作浏览器时，挑出破损的布局、不工作的按钮、不自然的操作流、不清楚的导航——这样的东西，并报告给你。不仅是漏洞修复，还会从"这里不好用"的视角给出建议。

是否采纳由你决定，但先听一下全部意见。

这结束后，就转到看不见的地方。

---

## 彻底杀死所有看不见的漏洞

看得见的漏洞可以修。问题是看不见的漏洞。

想想冰箱下面。从外面看得见的蟑螂只有一只。
但移开冰箱就会发现另一个世界。
软件也是一样。日志中没有的漏洞、无法重现的漏洞、没人踏过的漏洞——它们确实存在。人类不可能全部找出来。

MCP debug就是那个灭虫剂。

### 做法

这样告诉AI代理：

```
连接YU AI Manager的MCP，调试整个源代码。
用source_tree了解文件组织，然后用source_read一个个读。
报告所有潜在的漏洞、一致性问题、可能出错的地方。
```

AI读源代码，用MCP确认系统的实际状态，挖掘表面看不出的问题。
收到报告后就让它修复。

### 坚持提问

不要一次就结束。

当AI说"就这样"时，这样回复：

```
还有其他的吗？
```

重复这个。AI每次会挖得更深。
当它真的说"没有了"时，那多半是真的没有了。

固执不是美德。但对漏洞来说，固执就是正义。

---

## 公开前要做安全评审

如果你要公开Extension，事先要通过安全评审。

不难。很快就结束。

只需这样对AI代理说：

```
请对这个Extension（或代码）进行安全评审。
用MCP也要检查YU AI Manager的设置和沙箱信息。
用source_read读取相关文件，发现问题就全部报告。
```

YU AI Manager内置了Extension的代码扫描功能。
这在Extension加载时自动执行。重启服务器并加载一次Extension。

扫描会自动检测以下内容：
- 危险模块（`subprocess`, `ctypes`, `importlib`）
- 直接数据库操作（`sqlite3` — 使用SandboxedDB）
- 动态代码执行（`eval`, `exec`, `__import__`）
- 网络访问（`requests`, `urllib`等）

如果有重大问题，Extension加载会被拒绝。如果只是警告，会被加载但记录到日志中。
查看日志，发现问题全部修复。

既然你要发布在别人的系统上运行的代码，就要有这样的责任意识。

安全模型的详细内容，请读"[Extension的安全模型](docs/ja/help/developer/extension-security.md)"。

---

## 不要碰core

如果是Extension，你就在被保护的世界里。
但如果要改动保护体本身——core和built-in Extension——你必须记住，**这会影响整体，你自己也可能受害**。

如果你用的是Tauri版本，或者其他任何情况下，都不能从Claude Desktop碰core和built-in Extension。
不是"不应该"，而是**没有这个能力**。
API中不存在这个路径。看不见的东西碰不着。

如果真的需要碰，就用Python版本。就这样。

---

## 关于耐心

AI代理很强大，但不是魔法。有些问题需要多次尝试。

当你感到沮丧时：
- 退一步
- 重新读一下你说了什么
- 思考缺少什么信息
- 从另一个角度尝试

问题会得到解决。你需要的是明确的思维，不是吼。

---

## 最后

AI是智力的重机械。

有了重机械也操纵不了就只是块铁。操纵得了的人上去才能做出十几个人的工作量。但挖什么、在哪儿建，还是人来决定。工具再聪明，有目标的还是你。

作者在18天内边告诉AI该做什么边做出了这个软件。
所有功能、所有修复、所有设计决定都来自对话。

反过来说，这份文档里写的东西就足够做出这样规模的东西。

基础知识都很枯燥。
但那是堤防石垣的第一步。
石头的码法、角度的调整——那些在做的过程中会学到。
复杂困难的问题，最后也能解决。

但如果基础不扎实，再小的规模也会倒塌。

不要小看上面写的东西。
要坚实地基，最重要的是坚实自己的技术地基。

工具在这里。文档也在这里。

**Go for it.**
