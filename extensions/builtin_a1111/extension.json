{
  "name": "builtin-a1111",
  "version": "1.1.0",
  "description": "Automatic1111 / SD WebUI metadata extraction (builtin) - parses the Parameters text embedded in PNG/WebP/WebM and produces structured inspect output",
  "type": "importer",
  "category": "metadata",
  "entry": "a1111_parser.py",
  "hooks": [
    "on_scan_file",
    "on_build_sections"
  ],
  "has_blueprint": true,
  "blueprint_prefix": "/ext/a1111",
  "config": {
    "enabled": true,
    "priority": 100
  },
  "config_schema": {
    "parse_negative": {
      "type": "bool",
      "default": true,
      "label": "Also extract negative prompt",
      "cli_flag": "--parse-negative",
      "description": "Extract the negative prompt by separating out its section"
    },
    "parse_params": {
      "type": "bool",
      "default": true,
      "label": "Also extract generation parameters",
      "cli_flag": "--parse-params",
      "description": "Extract parameters such as Steps, Sampler, CFG Scale, and Model"
    }
  }
}
