<!-- GENERATED by scripts/gen_feature_index.py -- do not edit by hand. -->
# Features

46 built-in extensions. Configuration details per feature below; see the per-feature docs for full setup.

## ai

### builtin-analysis

AI image analysis engines (Claude, OpenAI, Ollama, Hailo VLM)


### builtin-audio-analysis

Audio transcription and analysis (Whisper local / OpenAI Whisper API)


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `engine` | select | `whisper_local` |  | Transcription Engine |
| `whisper_language` | text | `` |  | Language (blank=auto) |
| `whisper_model` | select | `base` |  | Whisper Model (local) |

### builtin-clip-coreml

CLIP Core ML encoder backend for semantic search (Apple Neural Engine)


### builtin-clip-onnx

CLIP ONNX Runtime encoder backend for semantic search


### builtin-clip-search

CLIP semantic image search (encoder abstraction, indexer, vector store)


### builtin-hailo-genai

Hailo-10H GenAI (LLM/VLM/Speech2Text)


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `default_llm_model` | str | `qwen3-1.7b-instruct` |  | LLM model to load by default |
| `default_vlm_model` | str | `qwen2-vl-2b-instruct` |  | Vision-Language model used when a chat request includes an image (qwen2-vl-2b-instruct or qwen3-vl-2b-instruct) |
| `max_generated_tokens` | int | `512` |  | Maximum number of tokens to generate per response |
| `temperature` | float | `0.7` |  | Sampling temperature (0 = deterministic, 2 = creative). HailoRT requires >0; values of 0 are automatically clamped to 0.01. |

### builtin-hailo-semantic-search

Semantic Image Search (Hailo / ONNX)


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `auto_index_on_scan` | bool | `False` |  | Automatically index new files after scan completes |
| `batch_size` | int | `128` |  | Number of images to process per batch during indexing |
| `preferred_backend` | select | `auto` |  | CLIP encoder backend: auto (best available), hailo, coreml, or onnx |
| `similarity_threshold` | float | `0.2` |  | Minimum cosine similarity score for search results (0.0-1.0) |

### builtin-hailo-yolo-detect

Hailo-10H YOLO Object Detection


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `auto_detect_on_scan` | bool | `False` |  | Automatically run object detection after scan completes |
| `backend` | select | `auto` |  | Inference backend. 'auto' detects the best available. |
| `batch_size` | int | `16` |  | Number of files to process per batch |
| `confidence_threshold` | float | `0.25` |  | Minimum confidence score for detections (0.05-0.95) |
| `model` | str | `yolov8n` |  | YOLO model variant (yolov8n, yolov11n, yolov5m) |
| `stream_max_sources` | int | `4` |  | Maximum number of simultaneous stream sources |
| `video_frame_interval` | float | `2.0` |  | Seconds between extracted video frames |

### builtin-inference

ONNX Runtime provider detection and GPU acceleration helpers


### builtin-ocr

VLM OCR - text extraction, structured document analysis, translation, and export


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `batch_export_format` | select | `md` |  | Batch Export Format |
| `batch_output_dir` | text | `` |  | Server-side save location for batch export. If blank, downloads as a ZIP |
| `default_target_lang` | enum | `en` |  | Default Translation Target Language |
| `default_task` | enum | `ocr` |  | Default OCR Task |
| `manga_ocr_enabled` | bool | `True` |  | Prefer manga-ocr (for the ocr_manga task) |

### builtin-speech-to-text

Speech-to-Text with auto backend detection (Hailo NPU / CUDA / ROCm GPU / CPU)


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `backend` | select | `auto` |  | Inference backend. 'auto' detects the best available. |
| `default_language` | str | `ja` |  | Default language code for transcription (e.g. ja, en) |
| `live_interval_sec` | float | `3` |  | Interval between transcription runs in live streaming mode |
| `model_size` | select | `base` |  | Whisper model size. Larger = more accurate but slower. |
| `stream_chunk_max_sec` | float | `10` |  | Maximum audio chunk duration (force-split if no silence) |
| `stream_chunk_min_sec` | float | `3` |  | Minimum audio chunk duration before splitting at silence |
| `stream_silence_ms` | float | `500` |  | Minimum silence duration to trigger chunk split |
| `stream_silence_threshold` | float | `500` |  | RMS threshold for silence detection in stream mode |

### builtin-video-analysis

Video AI analysis (multi-keyframe vision + optional Gemini direct video)


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `keyframe_count` | float | `4` |  | Keyframes to analyze |
| `strategy` | select | `uniform` |  | Keyframe Strategy |

### builtin-wd-tagger

WD-Tagger image auto-tagging (ONNX, VLM, composite engines)


## metadata

### builtin-a1111

Automatic1111 / SD WebUI metadata extraction (builtin) - parses the Parameters text embedded in PNG/WebP/WebM and produces structured inspect output


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `parse_negative` | bool | `True` | --parse-negative | Extract the negative prompt by separating out its section |
| `parse_params` | bool | `True` | --parse-params | Extract parameters such as Steps, Sampler, CFG Scale, and Model |

### builtin-annotations

File annotation storage, search, and batch operations


### builtin-comfyui

ComfyUI workflow JSON metadata extraction (builtin) - parses CLIPTextEncode + KSampler and produces structured inspect output


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `extract_workflow` | bool | `True` | --extract-workflow | Store the ComfyUI workflow JSON in raw_meta_json |

### builtin-novelai-v3

NovelAI V3-and-earlier metadata extraction (builtin) - supports the legacy prompt/uc format in PNG/WebP and produces structured inspect output


### builtin-novelai-v4

NovelAI V4 metadata extraction (builtin) - supports Character Prompts and Vibe Transfer in PNG/WebP and produces structured inspect output


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `extract_vibe_transfer` | bool | `True` | --extract-vibe | Extract the strength/description of Vibe Transfer |
| `merge_character_prompts` | bool | `True` | --merge-chars | Join base_caption + char_captions with commas |

### builtin-ratings

File star rating system (1-5 stars)


### builtin-tag-dictionary

Danbooru tag dictionary search, import, and split


## bridge

### builtin-comfyui-bridge

ComfyUI integration bridge


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `api_key_enc` | str | `` |  | Gateway API Key (encrypted) |
| `api_url` | str | `http://127.0.0.1:8188` |  | ComfyUI API URL |
| `auto_send` | bool | `False` |  | Skip confirmation dialog |
| `bridge_managed_save` | bool | `False` |  | Embed Sweep XMP directly into ComfyUI output (avoids double-saving) |
| `comfy_output_root` | str | `` |  | Sweep XMP embed target path (ComfyUI-side output directory) |
| `comfy_output_same_as_save_folder` | bool | `True` |  | Use save_folder as the Sweep XMP embed target |
| `default_image_format` | enum | `png` |  | Default Image Format |
| `default_sampler` | enum | `euler` |  | Default Sampler |
| `default_scheduler` | enum | `normal` |  | Default Scheduler |
| `gateway_url` | str | `` |  | Gateway URL (e.g. http://host:5000/comfy) |

### builtin-lora-dataset-manager

LoRA Dataset Manager -- kohya_ss / sd-scripts dataset generation


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `checkpoint_dir` | str | `` |  | Base model (checkpoint) directory |
| `default_base_model` | enum | `sdxl` |  | Default base model type |
| `kohya_path` | str | `` |  | kohya_ss path (parent of train_network.py) |
| `kohya_python_path` | str | `` |  | kohya_ss Python path (override, leave empty for auto-detect) |
| `output_base_dir` | str | `` |  | Dataset output base directory |
| `train_extra_args` | str | `` |  | Extra training arguments (space-separated, e.g. --optimizer_type=AdamW8bit) |
| `train_learning_rate` | str | `1e-4` |  | Learning rate |
| `train_max_epochs` | int | `10` |  | Max train epochs |
| `train_network_alpha` | int | `16` |  | Network Alpha |
| `train_network_dim` | int | `32` |  | Network Dim (LoRA rank) |
| `train_save_every_n_epochs` | int | `2` |  | Save every N epochs |

### builtin-nai-bridge

NovelAI API integration bridge


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `api_token` | str | `` |  | NAI API Token (pst-***) |
| `auto_send` | bool | `False` |  | Skip confirmation dialog |
| `cache_max_size_mb` | float | `500` |  | Vibe encode cache (MB) |
| `default_image_format` | enum | `png` |  | Default Image Format |
| `default_model` | enum | `nai-diffusion-4-5-full` |  | Default Model |
| `default_noise_schedule` | enum | `karras` |  | Default Noise Schedule |
| `default_sampler` | enum | `k_euler_ancestral` |  | Default Sampler |

### builtin-sd-webui-bridge

SD WebUI / Forge integration bridge


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `api_key_enc` | str | `` |  | Gateway API Key (encrypted) |
| `api_url` | str | `http://127.0.0.1:7860` |  | SD WebUI API URL |
| `auto_send` | bool | `False` |  | Skip confirmation dialog |
| `bridge_managed_save` | bool | `False` |  | Manage saving via the bridge (with Sweep XMP; disables SD WebUI-side saving) |
| `default_image_format` | enum | `png` |  | Default Image Format |
| `default_sampler` | enum | `Euler a` |  | Default Sampler |
| `gateway_url` | str | `` |  | Gateway URL (e.g. http://host:5000/sd) |

## prompt

### builtin-prompt-library

Prompt Library & Organization


### builtin-prompt-simulator

Dynamic Prompts simulator, emphasis weight calculator, and prompt conversion tools


### builtin-prompt-syntax

Prompt syntax highlighting, auto-detection, and error-detection engine - supports NAI/SD/DP syntax, bracket-depth color coding, and mixed-syntax warnings


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `auto_detect` | bool | `True` |  | Automatically detect NAI/SD/DP syntax in the input text |
| `show_errors` | bool | `True` |  | Display warnings for bracket mismatches and abnormal weight values |

### builtin-sd-nai-convert

SD <-> NovelAI prompt conversion - weight syntax, LoRA removal, and Dynamic Prompts mutual conversion + Web UI


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `convert_emphasis` | bool | `True` | --convert-emphasis | Also convert emphasis syntax between SD ((tag)) and NAI {tag} |
| `strip_embedding` | bool | `True` | --strip-embedding | Automatically remove embedding:... tags when converting SD to NAI |
| `strip_lora` | bool | `True` | --strip-lora | Automatically remove <lora:...> tags when converting SD to NAI |

## library

### builtin-chatlog

Chat log importer and viewer for Claude / ChatGPT


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `api_key` | str | `` |  | Required for OpenAI / Claude API. Leave blank for local Ollama |
| `base_url` | str | `http://localhost:11434` |  | API endpoint URL of the AI engine |
| `engine_type` | enum | `ollama` |  | Type of AI engine used for conversation analysis |
| `model` | str | `llama3.2:latest` |  | Name of the model to use (e.g. llama3.2:latest, gpt-4o-mini) |

### builtin-cross-search

Cross-text search across MD, chat logs, prompts and plain text files


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `txt_scan_roots` | array | `[]` |  | Directories to scan for .txt files (falls back to global scan_roots if empty) |

### builtin-download

Batch ZIP download for selected images


### builtin-export

Export hooks for record transformation during CSV/data export


### builtin-favorites-manager

Favorites & Collections Manager


### builtin-freeze-pullback




### builtin-lan-share

LAN collection sharing with time-limited token authentication


### builtin-md-viewer

Markdown file viewer with FTS5 full-text search


### builtin-stats

Statistics insights (personality analysis, timeline events, milestones)


### builtin-trophy

Trophy / achievement system with milestone tracking


## system

### builtin-auto-scan-watcher

Auto Scan Watcher - automatically detects file changes and applies incremental DB updates


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `auto_start` | bool | `True` |  | auto_start |
| `debounce_seconds` | float | `3.0` |  | debounce_seconds |
| `scan_archives` | bool | `True` |  | scan_archives |

### builtin-backup

Database backup, restore, scheduler, and event-driven auto-backup


### builtin-debug-check

Debug diagnostics CLI (DB, config, extensions, modules, syntax)


### builtin-github-integration

GitHub issue monitoring, triage, PR tracking, notifications, and reporting


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `auto_close_invalid` | bool | `False` |  | Auto-close invalid issues |
| `language_filter` | str | `en` |  | Language filter (e.g. 'en', empty=all) |
| `notify_on_connect` | bool | `True` |  | Notify pending issues on MCP connect |
| `poll_interval_minutes` | int | `60` |  | Issue poll interval (minutes) |
| `schedule_interval_minutes` | int | `0` |  | Schedule interval (minutes, 0=manual only) |

### builtin-lan-cowork

LAN peer discovery, prompt/wildcard sync, and distributed generation


| key | type | default | cli flag | description |
|-----|------|---------|----------|-------------|
| `discovery_port` | float | `19850` |  | discovery_port |
| `distributed_gen_enabled` | bool | `True` |  | distributed_gen_enabled |
| `enabled` | bool | `False` |  | enabled |
| `image_sync` | str | `immediate` |  | image_sync |
| `manual_peers` | array | `[]` |  | manual_peers |
| `peer_name` | str | `auto` |  | peer_name |
| `sync_enabled` | bool | `True` |  | sync_enabled |

### builtin-mcp-client

MCP Client -- connect to external MCP servers


### builtin-sns-share

SNS sharing (Bluesky, X/Twitter) with template expansion


### builtin-webhook

Webhook dispatcher with event-driven HTTP delivery and retry
