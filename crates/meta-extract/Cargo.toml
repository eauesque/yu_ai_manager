[package]
name = "meta-extract"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
thiserror = { workspace = true }
serde_json = { workspace = true }
# Lookbehind is needed for the bare `embedding:name` form, which `regex` cannot
# express; the prompt parsers already rely on the same crate in tagdb-core.
fancy-regex = { workspace = true }
exif = { package = "kamadak-exif", version = "0.5" }

[dev-dependencies]
tempfile = { workspace = true }
