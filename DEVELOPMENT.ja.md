# 開発ガイド

このソフトウェアを自分で拡張・カスタマイズ・デバッグするための案内書。

---

## 基本的な考え方

このソフトウェアは、人間がAIエージェントに指示と愚痴を言いながら作ったものだ。
コードは全行AIが書いた。

つまり：**あなたにも同じことができる。**

プログラマーである必要はない。作者に頼む必要もない。
必要なのは明確に考え、正確に説明し、繰り返す意志だけだ。

黒い画面に白い文字が流れるアレは必要ない。
まずその固定概念と偏見を捨てなさい。
すべてがビジュアルで完結できる、良い時代だ。

---

## 始める前に

### 先に読んでおくもの

API を追加・変更するつもりなら、先に [API セキュリティ指針](docs/ja/help/developer/api-security.md) を読みなさい。

- `GET` でも広く開けてはいけない
- `read-only API key` に何を残すか
- localhost 判定、secret、設定 API、validation の基準

この指針を読まずに route を増やすと、同じ事故を繰り返す。

### YU AI Managerを入手する

インストーラーを実行するだけだ。
あとは画面の指示に従いなさい。それで終わる。

ひとつだけ覚えておきなさい。
現時点では自動アップデートはない。新しいバージョンが出たら、同じようにインストーラーを実行して入れ替えなさい。

### MCPを接続する

YU AI Managerを開いて **Settings → API Keys** を見なさい。
**MCP Connection Snippet** というセクションがある。そこのJSONをワンクリックでコピーしなさい。

次にClaude Desktopを開いて **Settings（歯車）→ Developer → Edit Config** を開く。
コピーしたJSONを貼り付けて保存し、Claude Desktopを再起動する。

以上だ。それだけで接続できる。

**APIキーについて：** Snippetを使わず手動で設定する場合は、同じく **Settings → API Keys** でキーを作成しなさい。`sk_...` で始まるキーは作成直後に一度しか表示されない。必ずその場でコピーすること。

### 環境を確認しなさい

1. YU AI Managerが動いているか — 起動して確認
2. MCPサーバーが動いているか — Claude Desktopの設定で確認
3. AIエージェントが使えるか — Claude Desktop、またはそれに準ずるもの

以上だ。準備完了。

### Gitフックを有効化する（gitで開発・push するノードのみ・クローン後に一度）

このリポジトリをgitで扱う（コミットやpushをする）なら、クロード後に一度だけこれを実行しなさい。

```bash
git config core.hooksPath .githooks
```

これで `.githooks/` の pre-commit / pre-push 検査（lint・型・各種ドリフト検査）が有効になる。
**設定しないと検査が休眠する。** ロジックは正しくても関所として動かないので、ドリフトが混入しても止められない（実際にこれが原因でドキュメント索引が大量にズレたことがある）。

確認はこれでいい。`.githooks` が返ればよい。

```bash
git config --get core.hooksPath
```

各ノードで一度ずつ必要だ（gitのこの設定はクローンごとのローカル設定で、共有されない）。

---

## MCPを使え

MCPサーバーが動いているなら、必ず使いなさい。

YU AI ManagerにはAIエージェント向けのヘルプエンドポイントが用意されている。
MCPを通じてデータベース、ログ、設定、そして**ソースコードそのもの**に直接アクセスできる。
ブラウザのUIを介した説明より、MCPで直接見させた方が速く正確だ。

AIエージェントにこう伝えるだけでいい：

```
YU AI ManagerのMCPサーバーに接続してください。
ヘルプエンドポイントを確認して、何ができるか教えてください。
```

### ソースコードをMCPで読ませる

YU AI Managerにはソースコード参照ツールが組み込まれている。

- **source_tree** — ファイル構成をツリーで表示する
- **source_read** — 指定したファイルの内容を読む
- **source_search** — ソースコード全体をテキスト検索する

AIエージェントはこれらを使って、チャットの中でソースを直接読める。
GitHub Desktopでフォルダを開いてClaude Codeに渡す、といった手順は不要だ。

ソースを参照させたいときはこう言えばいい：

```
source_treeでファイル構成を確認して、
関係しそうなファイルをsource_readで読んでください。
```

---

## 機能を追加する

coreへの機能追加を作者に頼むな。答えはノーだ。

Extensionシステムを使いなさい。
**全ての作業がClaude Desktopのチャットで完結する。** 机を離れる必要はない。

### Step 1: 何を作るかをチャットで決めろ

いきなり「作って」と言うな。

まずClaude Desktopのチャットで、自分が何をしたいのかを整理しなさい。
「こういう機能が欲しい」「こういう操作を自動化したい」——それをAIと会話しながら言語化する。

何を作るか自分の中で明確になったら、こう言いなさい：

```
仕様書を作ってください。
```

AIが仕様書を作る。

### Step 2: そのまま作らせろ

作業場に移る必要はない。同じチャットでこう続けなさい：

```
仕様書ができました。Extensionとして実装してください。
create_extensionで雛形を作って、write_extension_fileでコードを書いてください。
validate_extensionで問題がないか確認してください。
```

AIはMCPを通じて、Extensionのファイルを直接作成・編集する。
あなたの机の上で、チャットだけで全てが完結する。

**ただし、その通りやるかどうかを決めるのはお前だ。**

AIの提案は参考にしなさい。しかし従う義務はない。
目的を持っているのはあなたであって、AIではない。
判断を渡すな。

合意できたら実装させなさい。おかしいところがあればそう伝える。動くまで繰り返す。

Extensionが完成したらYU AI Managerを再起動しなさい。
Settings → Extensionsに新しいExtensionが現れる。権限を確認して承認すれば動く。

### Step 3: 共有する（任意）

役に立つものを作ったなら共有しても構わない。
使うかどうかは他の人間が決める。We made, you decide.

---

## バグを報告する

### Step 1: ログを取れ

YU AI Managerを開いて **Settings → Logs** を見なさい。
問題が起きた前後のログをコピーする。

ログが見つからない場合は以下を正確に説明しなさい：
- 何をしたか
- 何が起きると思っていたか
- 実際に何が起きたか

「なんかおかしい」は説明ではない。

### Step 2: スクリーンショットか動画を撮れ

問題が視覚的なもので言葉では説明できないなら：

- **スクリーンショット**: `Windows + Shift + S`
- **画面録画**: `Windows + Shift + R`

Macの場合: スクリーンショットは `Cmd + Shift + 4`、録画は `Cmd + Shift + 5`

画像はそのままチャットにドラッグして渡せる。
混乱した説明千語より、一枚の画像の方がはるかに価値がある。

**ブラウザの中で何が起きているかも渡せる。**

ブラウザで `F12` を押しなさい。画面の端にパネルが開く。
今は意味が分からなくていい。ただ覚えておきなさい。

AIエージェントに「F12を開いてエラーを確認してください」と言われたとき、それがここだ。
赤いものと黄色いものが出ていたら、全部選択してコピーし、そのままエージェントに渡しなさい。
それだけでいい。

### Step 3: GitHubに貼れ

ログとスクリーンショットをGitHubのissueに貼っておきなさい。
作者が見るかもしれない。いつかは。保証はしない。

今すぐ直したいなら次のセクションへ進みなさい。

---

## 自分でバグを直す（推奨）

作者を待つより早い。本当に。

### 道具

**Claude Desktopのチャット＋MCP。** それだけだ。

考える・調べる・直す——全部ここで完結する。
MCPでExtensionのファイルを読み書きし、コードスキャンも実行できる。
他に何もいらない。

### デバッグの流れ

Claude Desktopのチャットで問題を説明しなさい。
ログ、スクリーンショット、何をしていたか、何を期待していたか——全部投げる。

MCPがあればAIはソースを直接読んで状態を確認できる。こう伝えなさい：

```
YU AI Managerで[X]をクリックすると[Y]が起きます。本来は[Z]になるべきです。
MCPでバックエンドのログと状態を確認してください。
source_treeとsource_readで関連するソースも読んでください。
原因を特定して修正してください。
```

AIが原因を特定して修正案を出す。
write_extension_fileで修正を適用し、validate_extensionで確認する。
YU AI Managerを再起動して動作を確認しなさい。

### AIエージェントに渡すもの

1. **エラーログ** — 言い換えではなく、そのままのテキスト
2. **スクリーンショットか動画** — 視覚的なバグの場合
3. **何をしていたか** — 問題が起きたときの操作
4. **何を期待していたか** — 本来どうなるべきだったか
5. **目的** — 症状だけでなく、なぜそれが必要なのか

### AIが理解してくれないとき

AIは人間ではない。あなたが省いた部分を補完してくれないことがある。

- 聞き返してくることもある — 正確に答えなさい
- 思った通りに動かないこともある — 何が違うか正確に伝えなさい
- 的外れな回答が続くなら言い方を変えなさい
- 情報が足りていないと気づいたら追加しなさい
- 言葉で伝わらないなら関連ファイルを投げなさい

これは反復作業だ。機能する。続けなさい。

人間に指示を出すときと本質は同じだ。ただし面子も機嫌も感情も関係ない分、ずっとシンプルだ。

---

## まず見えるところを綺麗にする

見えないバグを潰す前に、見えているところを片付けなさい。
雑草だらけのグラウンドに殺虫剤を撒いても意味がない。まず地面を整えることだ。

実装したものがある。動いているように見える。だがガワが本当に正しく動いているかどうかは、自分でマウスをカチカチやっても分からないことが多い。見落とす。慣れると気づかなくなる。

Playwrightを使いなさい。AIエージェントがブラウザを実際に操作して、UIを隅々まで確認してくれる。

AIエージェントにこう言いなさい：

```
PlaywrightでYU AI Managerを操作して、UI/UXのバグ出しと、
UXの面での評価と改善提案をしてください。
```

AIはブラウザを動かしながら、崩れているレイアウト、動かないボタン、不自然な操作フロー、分かりにくい導線——そういったものを拾い上げて報告してくれる。バグの修正だけでなく、「ここは使いにくい」という視点からの提案も来る。

受け入れるかどうかはお前が決めることだが、一度全部聞きなさい。

これが終わったら、次は見えないところへ進む。

---

## 見えないバグを全て殺す

目に見えるバグは直せる。問題は見えないバグだ。

冷蔵庫の下を考えなさい。表から見えているゴキブリは一匹だ。
だが冷蔵庫を動かすと、そこには別の世界がある。
ソフトウェアも同じだ。ログに出ないバグ、再現しないバグ、誰も踏んでいないバグ——それらは確かに存在している。人間がそれを全て探し出すのはほぼ不可能だ。

MCP debugはその殺虫剤だ。

### やり方

AIエージェントにこう言いなさい：

```
YU AI ManagerのMCPに接続して、ソースコード全体をデバッグしてください。
source_treeでファイル構成を把握して、source_readで順番に読んでいってください。
潜在的なバグ、整合性の問題、エラーになりうる箇所を全て報告してください。
```

AIはソースを読み、MCPでシステムの実際の状態を確認し、表に出ていない問題を掘り起こす。
報告が来たら修正させなさい。

### しつこく聞け

一度で終わりにするな。

AIが「以上です」と言ったら、こう返しなさい：

```
まだ他にありませんか？
```

これを繰り返しなさい。AIは毎回もう少し深く掘る。
本当に「もうない」と言ったとき、それはほぼ本当にないと思っていい。

しつこいのは美徳ではない。しかしバグに対してはしつこさが正義だ。

---

## 公開する前にセキュリティレビューをやれ

Extensionを公開するつもりなら、事前にセキュリティレビューを通しなさい。

難しくない。すぐ終わる。

AIエージェントにこう言うだけでいい：

```
このExtension（またはコード）のセキュリティレビューをしてください。
MCPでYU AI Managerの設定とサンドボックス情報も確認してください。
source_readで該当ファイルを読んで、問題があれば全て報告してください。
```

YU AI ManagerにはExtensionのコードスキャン機能が組み込まれている。
これはExtensionの読み込み時に自動的に実行される。サーバーを再起動してExtensionを一度読み込ませなさい。

スキャンは以下を自動検出する：
- 危険なモジュール（`subprocess`, `ctypes`, `importlib`）
- 直接のDB操作（`sqlite3` — SandboxedDBを使え）
- 動的コード実行（`eval`, `exec`, `__import__`）
- ネットワークアクセス（`requests`, `urllib` 等）

重大な問題があればExtensionの読み込みが拒否される。警告だけなら読み込まれるが、ログに記録される。
ログを確認して、問題があれば全て修正しなさい。

他人のシステムで動くコードを出すなら、それくらいの責任は持ちなさい。

セキュリティモデルの詳細は「[Extensionのセキュリティモデル](docs/ja/help/developer/extension-security.md)」を読みなさい。

---

## coreには触れるな

Extensionなら守られた世界にいられる。
守っている存在そのもの——coreとbuilt-in Extension——を変える場合には、それが全体に影響を及ぼし、**自分自身も被害を受ける可能性がある** ことをゆめゆめ忘れるな。

Tauri版を使っている場合や、そうでなくてもどんな場合でも、Claude Desktopからは、coreとbuilt-in Extensionに触ることはできない。
「してはいけない」ではなく、**能力として不可能** になっている。
APIにパスが存在しない。見えないものには触れない。

どうしても触る必要があるならPython版を使え。以上だ。

---

## 忍耐について

AIエージェントは強力だが魔法ではない。複数回の試行が必要な問題もある。

苛立ちを感じたとき：
- 一歩引きなさい
- 自分が何を伝えたかを読み直しなさい
- 何の情報が足りていないか考えなさい
- 別の角度から試みなさい

問題は解決する。必要なのは怒鳴ることではなく、明確な思考だ。

---

## 最後に

AIとは、智の重機だ。

重機があっても操縦できなければただの鉄の塊だ。操縦できる人間が乗って初めて、何十人分もの仕事をする。だが何を掘るか、どこに建てるかを決めるのは人間のままだ。道具が賢くなっても、目的を持つのはあなただ。

作者はこのソフトウェアをAIに何をすべきかを伝えながら18日間で作った。
すべての機能、すべての修正、すべての設計判断は会話から生まれた。

逆に言えば、このドキュメントに書いてあることだけで、それだけの規模のものが作れる。

基本はどれも退屈なことばかりだ。
だがそれが、堤防の石垣を積む第一歩になる。
石の積み方、角度の補正——それはやっていくうちに身につく。
複雑で困難な問題も、やがて解決できるようになる。

ただし、基本が疎かなら大した規模でなくても崩れる。

上に書いたことを馬鹿にしないことだ。
地盤を固くするには、おのれの技術の地盤を手堅くすることが最も重要だ。

道具はここにある。ドキュメントもここにある。

**Go for it.**
