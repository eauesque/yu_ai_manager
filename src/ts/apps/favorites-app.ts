/**
 * Favorites page bundle entry point.
 * Provides the subset of modules the favorites manager extension needs:
 * apiFetch, escapeHtml, showToast, tr, showDetail, closeModal,
 * navigateModal, updateModalNavButtons, updateModalFavButton,
 * loadSavedAnalysis, copyWithFeedback, notifyCopy, renderFileName,
 * renderPathDisplay, FocusTrap (via _ext_nav.html).
 */
import '../i18n/index';
import '../meta-renderer/index';
import '../detail-modal/index';
import '../runtime-pre/index';
import '../main/index';
import '../runtime-tools-ui/index';
