/**
 * sse module — Public API for the shared SSE connection
 */
export { sseSubscribe, sseUnsubscribe, _NativeEventSource } from './sse-shared';
export type { SseHandler, SseEventType } from './sse-shared';
