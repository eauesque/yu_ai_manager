"""Shim package: re-exports from extensions/builtin_clip_search/core_impl/.

The actual implementation lives inside the builtin-clip-search extension.
This package provides backward-compatible import paths so that other
extensions and core modules can continue to use ``from core.clip_core...``.
"""
