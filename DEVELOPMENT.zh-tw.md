# 開發指南

此軟體是為了讓您自行擴展、自訂和除錯而編寫的導覽。

---

## 基本概念

此軟體是一個人類在指示和抱怨給AI助手的過程中開發的。
所有程式碼都由AI編寫。

也就是說：**您也能做同樣的事。**

您不需要成為程式設計師。您不需要依賴作者。
您所需要的只是清晰的思考、精確的表達，以及重複嘗試的意願。

黑色螢幕上白色文字流動的東西不是必需的。
首先要拋棄這種固定觀念和偏見。
這是一個好時代，一切都可以視覺化完成。

---

## 開始之前

### 先讀的東西

如果您打算新增或修改API，請先閱讀[API安全指南](docs/ja/help/developer/api-security.md)。

- 即使是`GET`也不應該開放太寬
- 在`read-only API key`中應該保留什麼
- localhost判定、secret、設定API、驗證的標準

如果不讀這份指南就增加route，您會重複相同的錯誤。

### 取得YU AI Manager

只需執行安裝程式。
然後按照螢幕上的指示操作。就這樣。

只記住一件事。
目前沒有自動更新。當新版本發佈時，以同樣的方式執行安裝程式來更新。

### 連接MCP

打開YU AI Manager並查看**Settings → API Keys**。
有一個**MCP Connection Snippet**的部分。點擊一下複製那裡的JSON。

接下來打開Claude Desktop並打開**Settings（齒輪）→ Developer → Edit Config**。
貼上複製的JSON並儲存，然後重啟Claude Desktop。

就這樣。只要這樣就能連接。

**關於API金鑰：** 如果不使用Snippet進行手動設定，請同樣在**Settings → API Keys**中建立金鑰。以`sk_...`開頭的金鑰只會在建立後顯示一次。必須立即複製。

### 確認環境

1. YU AI Manager是否在執行 — 啟動並確認
2. MCP伺服器是否在執行 — 在Claude Desktop設定中確認
3. AI助手是否可用 — Claude Desktop或類似工具

就這樣。準備完成。

### 啟用Git掛鉤（只有使用git進行開發/push的節點·克隆後執行一次）

如果要用git處理此儲存庫（commit或push），克隆後請執行一次：

```bash
git config core.hooksPath .githooks
```

這樣`.githooks/`中的pre-commit / pre-push檢查（lint、類型、各種漂移檢查）就會啟用。
**如果不設定，檢查將休眠。** 即使邏輯正確，作為關卡也無法運作，所以漂移可能會混入（實際上這導致了文檔索引大量偏移的情況）。

確認可以這樣做。如果返回`.githooks`就可以了。

```bash
git config --get core.hooksPath
```

每個節點需要執行一次（git的這個設定是每個克隆的本地設定，不會共享）。

---

## 使用MCP

如果MCP伺服器在執行，一定要使用它。

YU AI Manager為AI助手提供了幫助端點。
透過MCP，您可以直接存取資料庫、日誌、設定，以及**原始程式碼本身**。
相比透過瀏覽器UI的說明，直接透過MCP展示要快得多、準確得多。

只需這樣告訴AI助手：

```
請連接YU AI Manager的MCP伺服器。
確認幫助端點，告訴我可以做什麼。
```

### 透過MCP讀取原始程式碼

YU AI Manager內建了原始程式碼參考工具。

- **source_tree** — 以樹狀形式顯示檔案結構
- **source_read** — 讀取指定檔案的內容
- **source_search** — 在整個原始程式碼中進行文字搜尋

AI助手可以使用這些工具在聊天中直接讀取原始碼。
不需要在GitHub Desktop中打開資料夾並將其傳遞給Claude Code的步驟。

當您想讓它查看原始碼時，可以這樣說：

```
使用source_tree確認檔案結構，
用source_read讀取相關的檔案。
```

---

## 新增功能

不要要求作者為core新增功能。答案是「不」。

使用擴充功能系統。
**所有工作都可以在Claude Desktop的聊天中完成。** 您不需要離開座位。

### 第1步：決定在聊天中要建立什麼

不要直接說「建立」。

首先在Claude Desktop的聊天中整理您想做的事。
「我想要這樣的功能」、「我想自動化這樣的操作」——與AI交談同時語言化這些想法。

當您清楚地知道要做什麼時，說這個：

```
請建立規格書。
```

AI會建立規格書。

### 第2步：讓它直接建立

不需要轉移到工作區。在同一個聊天中繼續這樣說：

```
規格書完成了。請將其實現為擴充功能。
用create_extension建立模板，用write_extension_file編寫程式碼。
用validate_extension確認沒有問題。
```

AI透過MCP直接建立和編輯擴充功能檔案。
一切都在您的座位上，只透過聊天完成。

**但是，是否按照這個方式進行由您決定。**

參考AI的提議。但您沒有義務遵循。
目標持有者是您，不是AI。
不要放棄判斷。

當達成共識時，讓它實現。如果有問題，就說出來。重複直到它工作為止。

擴充功能完成後，重啟YU AI Manager。
Settings → Extensions中會出現新的擴充功能。確認權限後，如果批准就會工作。

### 第3步：分享（可選）

如果您建立了有用的東西，可以分享。
其他人決定是否使用。We made, you decide.

---

## 報告錯誤

### 第1步：記錄日誌

打開YU AI Manager並查看**Settings → Logs**。
複製問題發生前後的日誌。

如果找不到日誌，請精確描述以下內容：
- 您做了什麼
- 您認為會發生什麼
- 實際上發生了什麼

「有些不對勁」不是描述。

### 第2步：拍攝截圖或影片

如果問題是視覺的，不能用文字描述：

- **截圖**: `Windows + Shift + S`
- **螢幕錄製**: `Windows + Shift + R`

在Mac上：截圖是`Cmd + Shift + 4`，錄製是`Cmd + Shift + 5`

您可以直接將圖像拖到聊天中。
一張圖片的價值遠大於混亂的千字說明。

**您也可以提供瀏覽器中發生的情況。**

在瀏覽器中按`F12`。螢幕的邊緣會打開一個面板。
現在不需要理解它的含義。只需記住。

當AI助手告訴您「打開F12並確認錯誤」時，就是這個。
如果出現紅色和黃色的東西，全部選中並複製，直接傳給助手。
就這樣。

### 第3步：貼到GitHub

將日誌和截圖貼到GitHub的issue中。
作者可能會看到。也許某時候。不保證。

如果想立即修復，請進到下一個部分。

---

## 自己修復錯誤（推薦）

比等待作者要快。真的。

### 工具

**Claude Desktop的聊天+MCP。** 就這樣。

思考、研究、修復——全部在這裡完成。
MCP可以讀寫擴充功能檔案，也可以執行程式碼掃描。
不需要其他任何東西。

### 除錯流程

在Claude Desktop的聊天中描述問題。
日誌、截圖、您在做什麼、您期望什麼——全部提供。

如果有MCP，AI可以直接讀取原始碼並確認狀態。這樣告訴它：

```
在YU AI Manager中點擊[X]會發生[Y]。本應是[Z]。
用MCP確認後端日誌和狀態。
用source_tree和source_read讀取相關原始碼。
確定原因並修復。
```

AI確定原因並提出修復方案。
用write_extension_file應用修復，用validate_extension確認。
重啟YU AI Manager並確認它工作正常。

### 提供給AI助手的內容

1. **錯誤日誌** — 原始文字，不是轉述
2. **截圖或影片** — 如果是視覺錯誤
3. **您在做什麼** — 發生問題時的操作
4. **您期望什麼** — 本來應該發生什麼
5. **目的** — 不只是症狀，為什麼需要它

### 當AI不理解時

AI不是人類。它可能不會補充您省略的部分。

- 它可能反問 — 精確回答
- 它可能不按預期工作 — 精確說出差異
- 如果持續給出離題答案 — 改變表達方式
- 如果您發現資訊不足 — 加上資訊
- 如果文字無法傳達 — 提供相關檔案

這是迭代過程。它會工作。堅持下去。

與人類給指示本質相同。但面子、心情、情感都無關，所以簡單得多。

---

## 先把看得見的地方清理乾淨

在消除隱藏的錯誤之前，把看得到的地方整理乾淨。
在雜草叢生的操場上撒殺蟲劑是沒有意義的。首先要整理地面。

您實現了某些東西。看起來在運作。但外觀是否真的正確運作，您無法透過自己點擊滑鼠確定。您會忽視。習慣後就不會注意。

使用Playwright。AI助手會實際操作瀏覽器，徹底檢查UI。

告訴AI助手這樣：

```
用Playwright操作YU AI Manager，進行UI/UX錯誤檢查，
並從UX角度進行評估和改進建議。
```

AI會在操作瀏覽器的同時，挑出破損的佈局、無法運作的按鈕、不自然的操作流程、令人困惑的導航——這些東西並進行報告。不只是修復錯誤，還會從「這個難用」的角度提供建議。

接受還是不接受由您決定，但要聽一次所有建議。

完成這個後，就進到隱藏的部分。

---

## 消滅所有隱藏的錯誤

看得見的錯誤可以修復。問題是看不見的錯誤。

想一下冰箱下面。表面上看得見的蟑螂只有一隻。
但當您移動冰箱時，底下是另一個世界。
軟體也一樣。日誌中不出現的錯誤、無法重現的錯誤、沒人踩到的錯誤——它們確實存在。人類不可能全部找出來。

MCP debug就是殺蟲劑。

### 方法

告訴AI助手這樣：

```
連接YU AI Manager的MCP，對整個原始程式碼進行除錯。
用source_tree掌握檔案結構，用source_read順序讀取。
報告所有潛在的錯誤、一致性問題、可能導致錯誤的地方。
```

AI讀取原始碼，透過MCP確認系統的實際狀態，發掘隱藏的問題。
收到報告後讓它修復。

### 執著地提問

不要一次結束。

當AI說「就這樣」時，這樣回應：

```
還有其他嗎？
```

重複這個。AI每次都會挖掘得更深。
當它真的說「沒有了」時，幾乎可以認為確實沒有了。

執著不是美德。但對於錯誤來說，執著是正義。

---

## 在公開之前進行安全性檢查

如果您打算公開擴充功能，請事先通過安全性檢查。

不難。很快就完成。

只需這樣告訴AI助手：

```
對此擴充功能（或程式碼）進行安全性檢查。
用MCP確認YU AI Manager的設定和沙盒資訊。
用source_read讀取相關檔案，報告所有問題。
```

YU AI Manager內建了擴充功能程式碼掃描功能。
這會在擴充功能載入時自動執行。重啟伺服器並讓擴充功能載入一次。

掃描會自動檢測以下內容：
- 危險模組（`subprocess`、`ctypes`、`importlib`）
- 直接的DB操作（`sqlite3` — 使用SandboxedDB）
- 動態程式碼執行（`eval`、`exec`、`__import__`）
- 網路存取（`requests`、`urllib`等）

如果有重大問題，擴充功能的載入會被拒絕。只有警告的話會載入，但會記錄在日誌中。
確認日誌，如果有問題就全部修復。

如果您要在別人的系統上執行程式碼，至少要有這樣的責任感。

安全模型的詳細資訊請參閱「[擴充功能的安全性模型](docs/ja/help/developer/extension-security.md)」。

---

## 不要接觸core

擴充功能內，您將處於受保護的世界。
如果要改變保護者本身——core和built-in擴充功能——千萬不要忘記，它會影響整體，**您自己也可能受害**。

如果您使用Tauri版本，或無論如何，也不能從Claude Desktop接觸core和built-in擴充功能。
不是「不應該」，而是**在能力上不可能**。
API路徑不存在。看不見的東西無法觸及。

如果您一定要接觸，請使用Python版本。就這樣。

---

## 關於耐心

AI助手很強大，但不是魔法。有些問題需要多次嘗試。

當您感到沮喪時：
- 後退一步
- 重新讀一遍您傳達的內容
- 思考缺少什麼資訊
- 從不同角度再試一次

問題會被解決。所需要的不是怒吼，而是清晰的思維。

---

## 最後

AI就是智慧的重機械。

即使有重機械，如果不會操縱也只是塊鐵。會操縱的人坐上去才能做十幾個人的工作。但要挖什麼、在哪裡建，仍然由人類決定。即使工具變聰明，有目標的還是您。

作者在18天內在指示AI該做什麼的同時完成了此軟體。
所有功能、所有修復、所有設計決定都來自對話。

反過來說，只要按照此文件中寫的東西，就能建造那種規模的東西。

基本都是無聊的事。
但那是堆砌堤壩石垣的第一步。
石頭的堆砌方法、角度的調整——那是在做的過程中學到的。
複雜困難的問題，最終也能解決。

但如果基礎不牢，再小的規模也會崩塌。

不要小看上面寫的東西。
要強化地基，最重要的就是堅實自己技術的地基。

工具在這裡。文件也在這裡。

**Go for it.**
